// Define a function named sum with two parameters x and y
function sum(x, y) {
    // Return the sum of x and y
    return x + y;
}

// Call the function sum with two arguments, 3 and 4
console.log(sum(3, 4)); // Output: 7
